<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Dice face (transparent, background handles body) -->
    <!-- Center dot -->
    <item
        android:top="72dp"
        android:bottom="72dp"
        android:left="72dp"
        android:right="72dp">
        <shape android:shape="oval">
            <solid android:color="@color/dice_dot"/>
        </shape>
    </item>
</layer-list>
